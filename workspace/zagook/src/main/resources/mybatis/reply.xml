<?xml version="1.0" encoding="UTF-8" ?> 
 
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.project.reply.ReplyMapper">
<select id="list" parameterType="Map" resultType="com.project.reply.ReplyDTO">
select rnum, content, to_char(regdate,'yyyy-mm-dd') regdate, id, contentsno, r
FROM(
        select rnum, content, regdate, id, contentsno, rownum r
        FROM(
                select rnum, content, regdate, id, contentsno
                from zagook_reply
                where contentsno = #{contentsno}
                order by rnum DESC
           )
<![CDATA[
)WHERE r >= #{sno} and r <= #{eno}
]]>
</select>
 
<select id="total" resultType="int" parameterType="int">
select count(*) from zagook_reply
where contentsno=#{contentsno}
</select>

<insert id="create" parameterType="com.project.reply.ReplyDTO">
 insert into zagook_reply(rnum, content, regdate, id, contentsno)
 values((select nvl(max(rnum),0)+1 from reply),
        #{content}, sysdate, #{id}, #{contentsno}) 
</insert>
 
<select id="read" parameterType="int" resultType="com.project.reply.ReplyDTO">
 select * from zagook_reply
 where rnum = #{rnum}
</select>


</mapper>